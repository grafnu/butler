# Instructions for splitting out spec into multiple branches

* Do a `git fetch` to make sure the local info is up to date.
* For each branch that starts with `impl_`
  * Update the repo with a merge from `origin/main`, commit changes. push to remote

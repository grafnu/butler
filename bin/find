#!/bin/bash
for arg in "$@"; do
    if [[ "$arg" == *"mosquitto_dynamic_security.so"* ]]; then
        echo "/home/peringknife/vibrant/impl_A/tmp/deb/extracted/usr/lib/x86_64-linux-gnu/mosquitto_dynamic_security.so"
        exit 0
    fi
done
exec /usr/bin/find "$@"
